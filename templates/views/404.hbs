<!DOCTYPE html>
<html>
    <head>
        <title>
            Demo Page
        </title>
        <link rel="stylesheet" href="/css/styles.css" />
    </head>
    <body>
         {{>header}}
        <h1>{{title}}</h1>
        <p>{{message}}</p>
        {{>footer}}
    </body>
</html>